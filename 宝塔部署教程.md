# Rove AI · 宝塔面板部署教程

本教程面向 **零基础** 用户，按顺序照做即可把系统部署到你的云服务器（Linux）。
全程约 20 分钟。遇到报错先看文末「常见问题」。

---

## 一、准备工作

1. **一台云服务器**（阿里云 / 腾讯云 / 华为云等均可）
   - 系统选 **CentOS 7+** 或 **Ubuntu 20.04+**
   - 配置建议 2 核 2G 及以上
2. **已安装宝塔面板**
   - 未安装的话，在服务器命令行执行官网给的一键安装脚本：https://www.bt.cn/new/download.html
   - 安装完记下面板地址、账号、密码
3. **服务器安全组放行端口**（重要！在云服务商控制台设置）
   - `8888`（宝塔面板）
   - `80`（网站）
   - `8000`（本项目后端，若用反向代理可不放行，见第五步）

---

## 二、在宝塔安装运行环境

登录宝塔面板后：

1. 左侧菜单 **软件商店** → 搜索并安装：
   - **Nginx**（任意版本，用于反向代理）
2. 安装 **Python 项目管理器**（推荐）
   - 软件商店搜索 “Python项目管理器”，安装
   - 它能帮你自动建虚拟环境、装依赖、守护进程，最省心

> 如果你更熟悉命令行，也可以跳过 Python 项目管理器，用第六步的「纯命令行方式」。

---

## 三、上传项目文件

1. 左侧菜单 **文件** → 进入 `/www/wwwroot/`
2. 新建目录，例如 `rove-ai`
3. 把项目的 **release 压缩包（zip）** 上传到该目录
4. 右键 → **解压**，确保目录里能看到 `main.py`、`requirements.txt`、`static`、`templates` 等文件

> 注意：解压后 `main.py` 要直接在 `/www/wwwroot/rove-ai/` 下，
> 如果多套了一层文件夹（如 `rove-ai/release/main.py`），把里面的文件移出来。

---

## 四、配置 .env（填入 API Key）

1. **文件** 管理器进入项目目录，找到 `.env.example`
2. 复制一份并重命名为 `.env`
3. 双击 `.env` 编辑，重点填写这几项：

```
SECRET_KEY=换成一段随便打的长字符串（越长越好）

LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
LLM_API_KEY=sk-你的DeepSeek密钥

SQLALCHEMY_DATABASE_URL=sqlite:///./app.db
```

> API Key 怎么获取：网页右上角「接入教程」有详细步骤，或参考项目里的说明。
> 数据库保持 SQLite 即可，无需额外安装。

---

## 五、用 Python 项目管理器启动（推荐方式）

1. 打开 **Python项目管理器** → 点 **添加项目**
2. 按下面填写：
   - **项目名称**：rove-ai（随意）
   - **项目路径**：选到 `/www/wwwroot/rove-ai`
   - **Python 版本**：选 **3.10 或更高**（列表里没有就先在管理器里「安装版本」）
   - **框架**：选 **python** 或 **其他**
   - **启动方式 / 启动文件**：选择 **uvicorn** 方式，或填自定义启动命令（见下）
   - **端口**：`8000`
3. **启动命令**（如果要手动填，用这条）：

```
uvicorn main:app --host 127.0.0.1 --port 8000
```

4. **安装依赖**：管理器一般会提示自动安装 `requirements.txt`；
   若没有，进入项目「模块管理 / 依赖」点安装，或在终端执行：

```
pip install -r requirements.txt
```

   国内服务器装得慢的话加镜像：

```
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

5. 点 **启动**，状态变成「运行中」即成功。

### 配置反向代理（让用户用域名/IP 直接访问，不用带 :8000）

1. 宝塔 **网站** → **添加站点**
   - 域名：填你的域名（没有域名就填服务器公网 IP）
   - 其余默认，提交
2. 进入该站点 → **反向代理** → **添加反向代理**
   - 代理名称：rove
   - 目标 URL：`http://127.0.0.1:8000`
   - 提交
3. 现在浏览器访问 `http://你的域名/chat` 就能打开系统了。

> 用了反向代理后，安全组只需放行 80 端口，8000 不用对外开放，更安全。

---

## 六、纯命令行方式（可选，不用 Python 项目管理器）

在宝塔 **终端** 里执行：

```
cd /www/wwwroot/rove-ai
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# 测试启动（Ctrl+C 可停止）
uvicorn main:app --host 127.0.0.1 --port 8000
```

测试能起来后，为了 **关闭终端也不停服**，用 nohup 后台运行：

```
cd /www/wwwroot/rove-ai
source .venv/bin/activate
nohup uvicorn main:app --host 127.0.0.1 --port 8000 > run.log 2>&1 &
```

停止服务：

```
# 查进程号
ps -ef | grep uvicorn
# 杀掉对应进程
kill 进程号
```

之后同样按第五步配置反向代理。

---

## 七、验证

浏览器打开 `http://你的域名/chat`（或 `http://服务器IP/chat`）：

- 页面能打开、能注册登录
- 发一条消息，AI 能正常回复 → 部署成功 🎉

---

## 常见问题

**Q1：页面能打开，但发消息报「AI出错了，请检查API Key」**
→ `.env` 里的 `LLM_API_KEY` 没填或填错，或账户没余额。重新检查并重启项目。

**Q2：页面打不开 / 502**
→ 后端没启动成功。检查 Python 项目管理器里状态是否「运行中」；
或看日志：项目目录下 `run.log`，或管理器的「日志」。

**Q3：pip 安装依赖很慢或失败**
→ 加国内镜像：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

**Q4：提示端口被占用**
→ 换个端口（如 8001），启动命令和反向代理目标 URL 同步改成 8001。

**Q5：想用 HTTPS（https 访问）**
→ 宝塔站点 → SSL → 申请 Let's Encrypt 免费证书，一键开启。

**Q6：数据存在哪？会丢吗？**
→ 用户和对话存在项目目录的 `app.db`（SQLite 文件）。做好定期备份这个文件即可。
